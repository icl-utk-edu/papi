stages:
  - pre
  - configure
  - make
  - test

job1:
  stage: pre
  script:
    - echo "TAG=hpsf-x86_64" >> job.env
    - echo "COMPONENT_NAME=cuda" >> job.env
  artifacts:
    reports:
      dotenv: job.env

job2:
  stage: configure
  script:
    - echo "Tag is $TAG and component is $COMPONENT_NAME"

    
