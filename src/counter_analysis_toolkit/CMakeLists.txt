
#TODO: just TODO, nothing there yet

# libraries with add_library(STATIC/SHARED)
# temporary archive with add_library( OBJECT )
# binaries with add_executable(...)
# don't forget to target_link_libraries(target PRIVATE/PUBLIC/INTERFACE)
# libraries should link in PUBLIC, binaries in PRIVATE
