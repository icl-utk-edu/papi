LIBRARY   = libpapi.a
SHLIB     = libpapi.so
SUBSTR  = x1
MSUBSTR  = x1
MEMSUBSTR= unicosmp
LIBS	= static  
TARGETS	= serial multiplex_and_pthreads 

TOPTFLAGS= -g
OPTFLAGS= -g
#OPTFLAGS= -O2
FOPTFLAGS= $(OPTFLAGS)
FTOPTFLAGS= $(TOPTFLAGS) 
FFLAGS	= $(FLAGS) 
SMPCFLGS= -mp
OMPCFLGS= -mp
CC_SHR  = cc
CC_R	= cc
CC	= cc
CPP	= cc -E
F77	= f77
POST_BUILD =
SPECIAL_REMOVE =

include ./Makefile.inc

native_clean:

native_install:

